{
  "private": true,
  "scripts": {
    "test": "spago test",
    "build": "spago build",
    "bundle": "spago bundle-app --to dist/app.js",
    "prepare-build": "cp public/index.html dist/index.html",
    "dev": "pnpm run prepare-build && nodemon & nano-server 8080 dist",
    "build:js": "esbuild --outdir=dist public/entry.js --bundle"
  },
  "devDependencies": {
    "esbuild": "^0.2.12",
    "nano-server": "^0.5.4",
    "nodemon": "^2.0.3"
  }
}
